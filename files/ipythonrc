# don't ask if we really want to exit
confirm_exit 0

# single line spacing, no extra empty lines
nosep 1

# always load autoreload on startup
# or manually: %load_ext autoreload
c.InteractiveShellApp.extensions = [
  'autoreaload'
]
